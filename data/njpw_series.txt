# NJPW series (tournament) post-IDs to pull from the official schedule API.
# One ID per line. Lines starting with # are ignored.
# The builder fetches https://app.njpw1972.com/series/tournaments/schedule/list/<ID>.json
# for each, which yields every show in that tour with confirmed bell times (English).
# Add a new line when NJPW announces a new tour (a few times a year); the URL/ID is the
# number in the series page URL, e.g. njpw1972.com/636143  ->  636143.
#
# The daily build ALSO auto-discovers near-term shows (incl. one-offs) from the public
# "njpwworld Schedule" Google Calendar, so new shows appear ~1 month out even before their
# series ID is added here — adding the ID just upgrades them to clean English + full horizon.

636143   # G1 Climax 36 (2026-07-11 .. 2026-08-16)
